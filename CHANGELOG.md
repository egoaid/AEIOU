# CHANGELOG

AEIOU Vocal Synthesizer — Christmas Edition の変更履歴。

---

## [v1.2] — EXTERNAL AUDIO SYNC / RANDOMIZE 追加

### 背景

v1.1(iOS Safari 安定化パッチ)公開後の機能追加セッション。以下2つの新機能を追加した。

### 追加機能

#### 1. EXTERNAL AUDIO SYNC(外部オーディオ同期再生)
- 複数のシンセ/ドラムマシーンWebアプリをブラウザの別タブで同時に鳴らし、
  まとめて1本の録音として書き出したい、という要望への対応。ブラウザの
  セキュリティモデル上、タブ間でオーディオクロックを共有することは
  不可能なため、代わりに「各アプリで個別に書き出した音源を、録音時に
  モニター専用として同時再生する」ことでテンポを間接的に揃えるアプローチ
  を採用した。
- 外部ファイル専用の `GainNode` を新設し、`AudioContext.destination`
  (スピーカー)にのみ接続、録音対象の `MediaStreamDestination`
  (`recDest`)には一切接続しない設計とした。これにより「耳には聞こえるが
  録音データには含まれない」を実現している。
- `MediaRecorder.start()` と外部ファイルの `AudioBufferSourceNode.start(0)`
  を同一のイベントハンドラ内で連続して呼ぶことで、同じ `AudioContext` の
  クロックに乗せて同時発火させ、録音開始との時間的ズレ(ドリフト)が
  蓄積しない設計にした。
- 録音とは独立した通常再生モード(▶ PLAY / ⏸ PAUSE / ■ STOP、進捗バー、
  経過時間/総時間表示)も搭載し、AEIOU本体の演奏と同時に鳴らすことも可能。
  `AudioBufferSourceNode` は仕様上使い捨て(pause不可)のため、一時停止/
  再生のたびにノードを作り直し、再生位置(秒)だけを引き継ぐ方式で実装。
- モニター音量調整スライダーを搭載(録音内容には影響しない)。
- 汎用性の高い機能のため、コア部分(`ExternalAudioSync`クラス)を
  依存ゼロの単一モジュールとして切り出し、他プロジェクトへの移植を
  容易にした(`external-audio-sync.js` / `external-audio-sync-panel.html`
  / `EXTERNAL_AUDIO_SYNC.md` として別途配布。本リポジトリには含まれない)。

#### 2. RANDOMIZE(全パラメーター一括ランダム生成)
- プリセットバーに 🎲 RANDOMIZE ボタンを追加。声質・エフェクトに関わる
  ほぼ全パラメーターを一度にランダム生成し、「予想もしない声」や
  「美しい偶然」を発見できるようにした。
- 対象: VOICE PARAMETERS 全般(TENS/RELEASE/VIBRATO/VIB REALISM/WOBBLE/
  RAWNESS/DYNAMICS/STRAIN/VEL GAIN/VOL/VOICE SIZE)、LEGATO、AUTO BREATH、
  CONSONANT ATTACK、HARMONY(SOLO/POLY)、VOICE CHARACTER(V2〜V4)、
  HUMANIZE、MOD WHEEL母音順、ADT & STEREO ECHO、VOICE DNA(V1〜V4個体差)、
  オクターブ、母音。
- 除外: EXTERNAL AUDIO SYNC(声のパラメーターではないため)、
  LF GLOTTIS RESEARCH パネル(実験用の隠しパネル。ランダム化すると
  破綻したノイズになりかねないため常に安全な `original` モードのまま
  固定している)。
- 実装は既存の `getState()`/`setState()`(プリセット読み込みの仕組み)を
  そのまま再利用。乱数で組み立てた「プリセット状態オブジェクト」を
  `setState()` に渡すだけで、UI・エンジン双方への反映ロジックを二重実装
  せずに済ませている。
- 各パラメーターは完全な一様乱数(スライダー全域)ではなく、既存29
  プリセット群で実際に使われている値に近い実用的なレンジでランダム化
  することで、「聴ける音」と「予想もしない意外な声」のバランスを取った。

### マニュアル更新
- 取扱説明書(日英)に「10. EXTERNAL AUDIO SYNC」セクションを新設し、
  以降のセクション番号(プリセット・画面表示・言語切替・仕様)を
  繰り下げた。
- 「プリセット」節に RANDOMIZE の説明を追記した。

### バージョン表記の統一
- `<title>` / `.logo-sub` / JSON-LD `softwareVersion` / マニュアルの
  サブタイトル / フッタークレジット、すべてを `v1.2` に統一した
  (v1.1で残課題として記録されていた `v1.0` 表記の残存は本リリースで解消)。

---

## [v1.1] — iOS Safari 安定化パッチ

### 背景

`v1.0` 公開後、iPhone SE (iOS Safari) で以下の症状が報告された。

- 起動直後、または一度無音になった後にリロードしても音が出ない
- 画面上のピアノ鍵盤で、押したキーが離せず鳴りっぱなしになる
- オクターブ切り替えボタンが反応しない
- （レイアウト変更後）鍵盤を1回タップしただけでは音が出ない
- 右手で鍵盤、左手でVOWELパッド（母音のXYパッド）を同時操作できない
- ホーム画面PWA化した状態で、アプリを終了→再起動すると音が出ないことがある

macOS Safari では終始問題が再現しなかった。iOS Safari だけでなく iOS 版 Chrome /
Brave（内部エンジンは全て WebKit）でも同じ症状が出ることを確認し、
**iOS の WebKit 実装特有の問題**であると特定した。

### 修正内容

#### 1. AudioContext のライフサイクル管理
- `pagehide` / `beforeunload` で `AudioContext.close()` を確実に呼ぶようにした。
  以前は close() されないままページを離れており、iOS ではオーディオセッションが
  ページ単位ではなく Safari プロセス単位で管理されているため、リロードを繰り返す
  うちに上限に達し、新しい AudioContext が無音のまま失敗する状態に陥っていた
  （この状態は端末のリロードでは直らず、Safari アプリ自体の完全終了が必要だった）。
- `visibilitychange` でバックグラウンド化時に `suspend()`、フォアグラウンド復帰時に
  `resume()` を試みるようにした。

#### 2. `interrupted` 状態からの自動復旧（Audio Watchdog）
- 実機デバッグの結果、iOS では `AudioContext` が生成直後から `interrupted` 状態の
  まま固まり、`resume()` の Promise が二度と解決しないケースがあることが判明。
- `resume()` を 700ms ごとに再試行し続ける **ウォッチドッグ** (`startAudioWatchdog`)
  を実装。
- 3回（約2秒）再試行しても復帰しない場合は、`AudioContext` を `close()` して
  `startInstrument()` を再実行し、音声グラフ（ScriptProcessor・4Voice・
  ADT/Echoエフェクト等）を丸ごと再構築する (`recreateAudioEngine`)。
- 20秒（30回）経っても復帰しない場合はウォッチドッグを一旦停止し、次の
  `noteOn` 操作時に再度監視を開始する。

#### 3. 固定 ms 待ちによるレースコンディションの解消
- 旧実装は「初回タッチ/クリック時は `startInstrument()` を呼んで固定 200ms
  （MIDI再生は300ms）待ってから `noteOn()` する」という実装だった。
  iOS の `resume()` 完了までの時間は端末の負荷や状況で変動するため、
  この固定時間待ちが「時々音が出ない」という不安定さの直接原因だった。
- `ensureAudioReady(callback)` ヘルパーを新設。`resume()` の Promise が実際に
  解決するのを待ってから発音し、400ms 経っても解決しない場合のみタイムアウト
  フォールバックとして発音を試みる（さらにこの場合は裏で watchdog を起動する）。
- 鍵盤タッチ／マウスクリック／QWERTYキー／MIDI再生の4箇所すべてをこの
  ヘルパー経由に統一した。

#### 4. 起動準備中バッジの追加
- AudioContext が `running` になるまでの間（通常は一瞬だが、上記の
  `interrupted` 復旧が絡む場合は最大 1 秒程度かかることがある）、ヘッダーに
  「🔈 音声準備中…」バッジを表示し、ユーザーが「壊れている」と誤解しない
  ようにした。`doScriptProcessor` の初回コールバック発火、または watchdog が
  `running` を確認した時点で自動的に消える。

#### 5. 画面ピアノ鍵盤の `touchcancel` 未処理
- 指がわずかに動くと iOS は「横スクロール」と判定し、`touchend` の代わりに
  `touchcancel` を発火させることがある。これが未処理だったため、該当ノートの
  `noteOff()` が呼ばれず「キーが押しっぱなしで鳴り止まない」状態になっていた。
- `touchcancel` を `touchend` と同じ解放処理に接続。
- `touchNoteMap`（タッチID→ノートのマップ）をグローバルスコープへ引き上げ、
  `visibilitychange` でバックグラウンド化した際にも `releaseAllTouchNotes()`
  で強制解放するようにした。

#### 6. 画面ピアノ鍵盤の横ドラッグ・スクロール対応
- 従来は `touchstart` で一律 `e.preventDefault()` していたため、iOS では
  ネイティブの横スクロール（`overflow-x:auto`）が完全に無効化されていた。
- 移動量が閾値（8px）を超えた指だけを「ドラッグ」と判定し、その指が
  鳴らしていたノートを解放したうえで `piano-wrap.scrollLeft` を手動で
  指の動きに追従させる方式を実装。閾値未満は通常の押鍵として扱う。

#### 7. オクターブボタン／Z・Xキーの挙動统一
- マウスクリック演奏・タッチ演奏は「見えている鍵をそのまま弾く」方式のため、
  従来 OCT ボタン（▼/▲）・Z/Xキーは QWERTY 演奏にしか影響しなかった。
- `scrollPianoToOctave(oct)` を実装し、オクターブ変更時に該当オクターブの
  C音が画面中央に来るよう鍵盤を自動スクロールすることで、マウス/タッチ演奏
  でもオクターブ切り替えが実質的に機能するようにした（OCTボタン・Z/Xキー・
  プリセット読み込み時のオクターブ復元、いずれからも呼び出される）。

#### 8. マルチタッチ対応（VOWELパッド × 鍵盤の同時操作）
- VOWEL/TONGUE POSITION の XY パッド（母音操作）が `e.touches[0]`
  （画面上で現在押されている全タッチのうち最初のもの）を無条件に使用していた
  ため、右手で鍵盤を押しながら左手でパッドに触れると、パッドが鍵盤側の
  タッチ座標を誤って読んでしまい正しく反応しなかった。
- 鍵盤側と同様、パッド自身が担当するタッチの `identifier` を記録・追跡する
  方式に変更し、他要素へのタッチと混ざらないようにした。

#### 9. 初回タップ無音バグ（レイアウト変更に伴う顕在化）
- `touchstart` ハンドラが、AudioContext 未起動時は `startInstrument()` を
  呼ぶだけで、タップしたキー自体の `noteOn()` を一度も呼んでいなかった
  （2回目以降のタップからしか鳴らない仕様になっていた）。
- 鍵盤セクションを画面上部（雪だるまシアター直下）へ移動したことで、多くの
  ユーザーが「START ボタンを押さずにいきなり鍵盤を1回だけタップする」動線に
  なり、この既存の欠陥が表面化した。
- `mousedown` ハンドラと同様、`ensureAudioReady()` 経由でタップしたノートを
  確実に鳴らすよう修正。

#### 10. オンスクリーン・デバッグログパネル
- Mac 実機に接続できない状況（iPhone 単体）でも原因を特定できるよう、
  画面上に表示できるデバッグログ機構を追加。
  - `window.onerror` / `unhandledrejection` を捕捉し、JS例外を自動表示
  - AudioContext の状態遷移・`ensureAudioReady`／watchdog の各判断・
    `noteOn()` 呼び出し・`doScriptProcessor` 初回発火を全てタイムスタンプ
    付きで記録
  - ログはコピー用ボタンからテキストとしてコピー可能
- **公開版では非表示がデフォルト。** URL に `?debug=1` を付けてアクセスした
  場合のみ表示され、以後は `localStorage` に記憶されて同一端末では
  パラメータなしでも表示され続ける。`?debug=0` でいつでも非表示に戻せる。
  ログ収集自体（`dbg()`）は非表示時も常時バックグラウンドで継続している。

### UI/レイアウト変更（バグ修正とは別の要望対応）

- ヘッダーから「PINK TROMBONE ENGINE」の文字列を削除（起動ステータスドット
  自体は JS から参照されているため、テキストなしで維持）。
- 「📖 取扱説明書」ボタンをヘッダーからフッター（credit部分）へ移動。
- 鍵盤セクションを雪だるまシアター（`#charWrap`）の直後、コントロール
  グリッドより前に移動（iPhoneで最下部までスクロールせずに演奏できるように）。

### 既知の残課題 / 申し送り事項

- Audio Watchdog は `noteOn` 経由でのみ起動する設計のため、ユーザーが
  一度も鍵盤に触れないまま `interrupted` に固まった場合、その後
  `visibilitychange` か `statechange` イベントが発生するまで自動復旧が
  始まらない。実運用上は大きな問題にならない想定だが、気になる場合は
  `startInstrument()` の呼び出し直後からウォッチドッグを仕込む案がある
  （本セッションの最後で議論したが未実装）。
- 本セッションのデバッグは iPhone SE (iOS 18.7 / Safari 26.5系) 1台での
  実機テストに基づく。他機種・他OSバージョンでの追加検証は未実施。
